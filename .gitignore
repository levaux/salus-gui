# dependencies
node_modules/
.pnpm-store/

# build output
dist/
build/
.svelte-kit/
*.tsbuildinfo

# test
test-results/
coverage/

# tooling
__pycache__/

# bridge runtime state
apps/bridge/data/
apps/bridge/certs/
*.pem

# env / local
.env
.env.*
!.env.example
.DS_Store

# editor
.vscode/*
!.vscode/extensions.json
.idea/

# Bridge audit log (durable NDJSON, per-site)
apps/bridge/audit/

# Bridge-local KV store (named workspaces, saved views)
apps/bridge/kv/

# generated proto codegen (rebuilt by `pnpm gen`; the proto package postinstall
# runs it on a fresh checkout). The vendored .proto sources and the buf breaking
# baseline image stay tracked — the baseline is recorded state, not derivable output.
packages/proto/src/gen/
